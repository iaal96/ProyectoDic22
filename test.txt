programa myprog;
var int i, j, k;

funcion int uno(int c, int d) {
    var char x, y;
    x = 'a';
    y = 'b';
}

funcion int dos(int a, int b) {
    var char x, y;
    x = 'g';
    y = 'h';
}

principal() {
    var int c;
        float k;
    c = 4;
    k = 1 + (2 - 3) * 4 / c;
    si (1 > 2) entonces {
        lee(c);
    } sino {
        lee(k);
    }
    mientras (1 < 3) {
        lee(k);
    }
    para c = 1 hasta c < 10 {
        lee(j);
    }
    c = 2 < 1;
    c = 1 | 0;
    lee(i);
    uno(1, 5);
    dos(3, 4);
}