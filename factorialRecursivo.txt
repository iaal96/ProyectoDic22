programa factorialRecursivo;

funcion int factorial(int x, int y) {
    si (x > 1) entonces {
        regresa(x * factorial(x - 1));
    }
    regresa(1);
}

principal() {
    var int y, x;
    x = factorial(x);
    imprime(x);
}