programa factorialCic;

principal() {
    var int resultado, num, x, a, b, c, y;
    resultado = 1;
    lee(num);
    para x = 1 hasta x <= num {
        resultado = resultado * x;
    }
    imprime(resultado);
    lee(y);
    raizcuadrada(y);
    lee(a);
    lee(b);
    lee(c);
    cuadratica(a,b,c);
}